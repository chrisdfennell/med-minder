# Normalize line endings: LF in the repo for text, CRLF for Windows scripts.
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Treat these as binary (no EOL conversion / diffing).
*.png binary
*.der binary
*.iq binary
*.prg binary
